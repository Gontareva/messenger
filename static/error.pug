extends components/layout/layout

block content
    - error = error || {code: 404, message: 'Не туда попали'};

    .page
        .error
            h1.error__code #{error.code}
            .error__message #{error.message}

            .error__link
                a(href='/') Назад к чатам
